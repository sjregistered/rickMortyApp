import { Character } from "./Character";

/**
 * CharacterCardProps interface defines the props for the CharacterCard component.
 * It includes a single property, character, which is of type Character.
 */

export interface CharacterCardProps {
  character: Character;  // The character data to be displayed in the card
}
