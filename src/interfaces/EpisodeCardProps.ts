import { Episode } from "./Episode";

/**
 * EpisodeCardProps interface defines the props for the EpisodeCard component.
 * It includes a single property, episode, which is of type Episode.
 */

export interface EpisodeCardProps {
  episode: Episode;  // The episode data to be displayed in the card
}
