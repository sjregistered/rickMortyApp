import { Location } from './Location';

/**
 * LocationCardProps interface defines the props for the LocationCard component.
 * It includes a single property, location, which is of type Location.
 */

export interface LocationCardProps {
  location: Location;  // The location data to be displayed in the card
}
